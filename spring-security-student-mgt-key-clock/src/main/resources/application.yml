spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: spring-boot-client
            client-secret: IuCXVLYZeRczi5WVXDeAesmYY9Vb0nvb
            scope: openid
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/keycloak"
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/myrealm
  datasource:
    url: jdbc:postgresql://localhost:5432/abc_spring_security
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081